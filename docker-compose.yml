services:
  bot:
    build: .
    container_name: socialvideodownload
    env_file: .env
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./downloads:/app/downloads
      - ./logs:/app/logs
    restart: unless-stopped
